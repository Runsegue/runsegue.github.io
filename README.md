# Runsegue website — GitHub Pages (Jekyll)

Push to `main`. Enable Pages: Settings → Pages → Source: GitHub Actions or branch `main` / root.

Local preview:

```bash
bundle install
bundle exec jekyll serve
```

Live: https://runsegue.github.io
